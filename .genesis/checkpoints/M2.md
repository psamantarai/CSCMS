# M2 — 9.5.16 Sweep

## G0 · 2026-08-08
- wiki pages read: `.genesis/wiki/index.md` — no dedicated cleanup/sweep concept page; `Boundary-Lines.md`
  pointer re-confirmed (presentation-only invariant, already known from M1).
- implementation-notes searched for: "colorMap", "inputStyle", "labelStyle", "cardStyle", "style={{" — none found (file still unfilled template).
- codebase grep/query:
  - `graphify query` for colorMap/inputStyle/labelStyle/cardStyle/style — only hit is `colorMap` in
    `src/components/StatCard.tsx:12`, a **live** semantic color map (`bg-success/5` etc, dark-mode-safe
    Tailwind classes) used by every Dashboard stat card — not dead code, despite the "colorMap-era
    constants" wording in docs/PLAN.md 9.5.16 (that wording predates the 9.5.10-9.5.14 shadcn rebuild).
  - `grep "style={{"` across `src/**/*.tsx` — 2 hits, both CSS custom-property patterns
    (`App.tsx:115` `--sidebar-width`, `toggle-group.tsx:43` `--gap`), not hardcoded colors. Zero hits
    in `Reports.tsx` — the `inputStyle`/`labelStyle`/`cardStyle` locals PLAN.md cites at
    `Reports.tsx:35-37` no longer exist (already removed by the 07921d4 Reports rebuild commit).
  - `index.css` read in full (257 lines) — every custom class (`.rs-grid`, `.rs-flex-split`,
    `.app-topbar`, `.no-print`, `.print-report`, `.mono`) still referenced somewhere in `src/**`
    (confirmed via grep, 17 files match) — no dead rules to remove.
  - One-off `tsc --noUnusedLocals --noUnusedParameters` sweep (not persisted to tsconfig, just a
    diagnostic run) — 2 hits:
    - `src/pages/Customers.tsx:7` — `TableRowState` imported from `QueryState`, never used. **Real
      migration orphan, in scope.**
    - `src/components/ui/scroll-area.tsx:3` — unused `React` import. This is a shadcn-generated
      primitive (new JSX transform doesn't need the import), not something CSCMS's page migration
      orphaned — out of scope per CLAUDE.md's Surgical Changes rule ("don't remove pre-existing dead
      code unless asked"); noting it, not touching it.
- decisions that bind us: `phase_9.5_presentation_only` invariant (src/** only)
- verdict: **PARTIAL** — the bulk of the sweep was already done by the prior shadcn-rebuild commits;
  remaining scope narrowed to: remove the one unused import in `Customers.tsx`, then run the full
  demo command + manual click-through to confirm nothing else survived.

## iter 1 · 2026-08-08
- gate G1: pass · skills: ponytail (session-active) + shadcn + vercel-react-best-practices (per DONE.html §4 for M2)
- gate G2: pass · `src/pages/Customers.tsx` -1 import (`TableRowState` removed, unused)
- gate G3: pass · well under 50000 token budget
- gate G4: pass · `npx tsc --noEmit` clean; `npx vite build` clean (only the pre-existing >500kB
  chunk-size notice, unchanged from M1); `style={{` count = 2 (`App.tsx` `--sidebar-width`,
  `toggle-group.tsx` `--gap`, both CSS custom properties, not colors — the deliberate survivors);
  unused-locals recheck clean except the out-of-scope `scroll-area.tsx` noted in G0
- gate G5: see L4 VERIFY below
- next: L4 VERIFY

## L4 VERIFY · 2026-08-08
- Checker: fresh-context general-purpose subagent (separate from maker), given the milestone goal +
  Verify line + the one-line diff + `phase_9.5_presentation_only` invariant text only — no build
  trail. Independently re-derived every G0 claim from the live repo rather than trusting it.
- Verdict: **APPROVE**
- Reasoning: confirmed independently — `style={{}}` has exactly 2 hits, both CSS custom properties
  (`--sidebar-width`, `--gap`), no hardcoded colors; `inputStyle`/`labelStyle`/`cardStyle` zero hits
  anywhere; the sole `colorMap` (StatCard.tsx) is a live semantic-class mapper, not an orphaned
  constant; `index.css` has no unreferenced rules; the `TableRowState` removal from Customers.tsx is
  safe (unused there, still used and unaffected in 9 other files); `tsc --noEmit` clean; invariant
  holds (diff confined to one presentation file). Noted the plan's original "print-report layout"
  survivor expectation is stale — that class is now applied via `className`, not inline `style`, so
  reality is cleaner than the plan anticipated, not a violation.

## Manual Verify (live browser, Chrome automation) · 2026-08-08
- Logged in as admin (default dev seed credentials from `backend/app/seed.py`) — Chrome extension's
  computer-tool clicks weren't registering keystrokes into the React-controlled login inputs this
  session (visible "admin"/dots in screenshots were a stale autofill overlay, not real DOM values,
  confirmed via `javascript_tool`); worked around it by setting input values through the native
  React-compatible setter + `input` event and submitting via `form.click()` — a one-off session
  workaround, not an app bug.
- Click-through, checking `read_console_messages(onlyErrors: true)` fresh after each navigation:
  Dashboard, Customers, Services, Transactions, Banking, Expenses, Accounts, Ledger, Reports,
  Closing, Audit Log — **zero console errors on any page** (11 routes + Login = 12, matching M1's page
  count). Screenshot capture (`computer` tool) was flaky/timing out this session (CDP timeout) — not
  used as evidence; console-error-free navigation was the actual signal, matching the Verify line's
  wording ("no console errors").

## Quiz-me gate (Q+A block) · 2026-08-08
1. **Design decision** — "G0 found the milestone's premise was already resolved before this session.
   Why didn't the loop just skip M2 and mark it BUILT?" → **Answer: one real orphan remained (correct)**
   — a one-off `tsc --noUnusedLocals` check surfaced `TableRowState` in Customers.tsx, missed by the
   earlier rebuild commits; G0 verdict was PARTIAL (narrowed scope), not BUILT (halt).
2. **Edge case** — "Why was StatCard.tsx's colorMap left alone?" → **Answer: it's live, dark-mode-safe
   code (correct)** — a semantic Tailwind-class mapper using theme tokens, not a hardcoded-color relic.
3. **Change impact** — "What's the practical effect of the one-line Customers.tsx diff?" →
   **Answer: none — pure dead-code removal (correct)** — TableRowState was unused there; removal
   changes zero runtime behavior.
- Verdict: **APPROVE** (3/3 correct)

## Result
- Demo command (`npx tsc --noEmit && pnpm build && grep -rc "style={{" src --include=*.tsx`): **PASS**
  — `tsc` clean, `vite build` clean (pnpm not on PATH, same substitution as M1), `style={{` count = 2
  (`src/App.tsx:1`, `src/components/ui/toggle-group.tsx:1`) — both CSS custom-property survivors, no
  hardcoded colors. Count recorded in `docs/progress-tracker.html`.
- Manual success criteria (docs/PLAN.md 9.5.16 Verify): **PASS** — 12-page click-through, no console
  errors.
- Quiz-me: **APPROVE** (3/3 correct).
- Milestone status: **DONE**
