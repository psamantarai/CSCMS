# M1 — 9.5.15 Dark mode

## G0 · 2026-08-08
- wiki pages read: `.genesis/wiki/index.md` (no dark-mode/theme concept page exists yet — nothing to drill into)
- implementation-notes searched for: "dark", "theme", "toggle" — none (file is still the unfilled template)
- codebase grep: `dark|Theme|theme` across `src/**/*.tsx` — 8 hits, all shadcn primitive internals (toggle.tsx, dropdown-menu.tsx, etc — unrelated `data-*`/aria naming, not an existing feature); `localStorage|classList.toggle` — 2 hits, both comments in `lib/auth.tsx`/`lib/api.ts` explicitly saying the *session token* stays in-memory, never localStorage (ARCHITECTURE.md §9) — confirmed this is a different concern from a theme-preference key, not a conflicting rule
- decisions that bind us: `phase_9.5_presentation_only` invariant (src/** only, no lib/api.ts/lib/queries.ts/lib/format.ts/mutation body)
- verdict: **UNBUILT** — no existing dark-mode toggle; `.dark` CSS token block already existed in `src/index.css` from the original shadcn init, ready to be wired up

## iter 1 · 2026-08-08
- gate G1: pass · skills: ponytail (session-active) + shadcn + frontend-design (per DONE.html §4 for M1); genesis's `agentic-swe-master`/`coding-orchestrator` canon skills are not installed in this project (DONE.html §4 note) — used the project's real skill roster instead
- gate G2: pass · `src/App.tsx` +19/-4 LOC: theme state lifted to `App()` (always-mounted, covers Login too), Sun/Moon toggle button added to `AppSidebar`'s footer next to the user block
- gate G3: pass · well under 50000 token budget
- gate G4: pass · `npx tsc --noEmit` clean; `pnpm build` unavailable (pnpm not on PATH in this shell — ran `npx vite build` instead, identical to the `build` script's actual command) — clean, no warnings besides the pre-existing >500kB chunk-size notice
- gate G5: see L4 VERIFY below
- decision: state lives in `App()` not `AppShell()`, because `AppShell` early-returns `<Login/>` before mount when unauthenticated — the theme class must apply regardless of auth state, confirmed by reload test (Login rendered dark)
- next: L4 VERIFY

## L4 VERIFY · 2026-08-08
- Checker: fresh-context general-purpose subagent (separate from maker), given goal + Verify line + diff + `phase_9.5_presentation_only` invariant text only — no build trail.
- Verdict: **APPROVE**
- Reasoning: `.dark` tokens in `index.css` cover every class the toggle/sidebar/shadcn components use; diff confined to `src/App.tsx` only (invariant holds); `localStorage["theme"]` is unrelated to the in-memory-only session-token rule; toggle mechanism structurally matches `@custom-variant dark (&:is(.dark *))`. One non-blocking nit: `useEffect` (not `useLayoutEffect`) means a returning dark-mode user could see a one-frame light flash before the effect fires on reload — not tested for in the Verify line, logged here as a known, accepted trade-off (ponytail: not worth `useLayoutEffect`'s complexity for a one-frame SPA flash; revisit only if it becomes visibly annoying).

## Manual Verify (live browser, Chrome automation) · 2026-08-08
- Logged in as admin, toggled dark from the sidebar footer.
- All 10 sidebar pages (Dashboard, Customers, Services, Transactions, Banking, Expenses, Accounts, Ledger, Reports, Daily Closing, Audit Log — 11 actually, see note) confirmed dark and readable via screenshot.
- 2 Dashboard modals (New Transaction, Record Expense) confirmed dark and readable (first screenshot of New Transaction caught a stale mid-animation frame showing white — re-screenshot and a DOM/computed-style check via `javascript_tool` confirmed the actual rendered background was correctly `rgb(16,30,48)` = `--popover` dark token the whole time; this was a screenshot-capture artifact, not an app bug).
- Reload persistence: reloaded mid-session (logs out per the existing in-memory-session design, unrelated) — Login page itself rendered dark, confirming the `localStorage` choice survived; signed back in — Dashboard resumed dark.
- Toggled back to light — clean, no leftover dark-only styling.
- Note: PLAN.md's "12 pages" count includes Login (not sidebar-gated) + the 11 authenticated routes I get by listing `navGroups` (Dashboard/Customers/Services/Transactions/Banking/Expenses/Accounts/Ledger/Reports/Closing/AuditLog = 11) + Login = 12. All checked.

## Quiz-me gate (Q+A block) · 2026-08-08
Re-posed this session after a `/clear` wiped the prior session's quiz record before it could be
logged (per LOOPS.md, the milestone is not done until the Q+A block is written to the checkpoint —
it wasn't, so re-asking was correct rather than assuming it happened).

1. **Design decision** — "Why does the theme state live in `App()` rather than `AppShell()`, where
   the sidebar/toggle actually renders?" → **Answer: don't know / skip.**
   (Correct answer: `AppShell` early-returns `<Login/>` before mounting when unauthenticated, so
   state there wouldn't reach the Login screen — confirmed live by the reload test.)
2. **Edge case** — "On a reload with `theme: dark` already in localStorage, what's visible for a
   split second before the page settles?" → **Answer: don't know / skip.**
   (Correct answer: a one-frame light flash, because the class is applied via `useEffect` not
   `useLayoutEffect` — a known, accepted trade-off logged in L4 VERIFY above.)
3. **Change impact** — "The diff touched only `src/App.tsx` (+19/-4). What real constraint did
   that satisfy?" → **Answer: don't know / skip.**
   (Correct answer: the `phase_9.5_presentation_only` invariant — src/** only, no
   lib/api.ts/lib/queries.ts/lib/format.ts/mutation body.)

Per LOOPS.md: any "don't know"/"skip" answer downgrades the verdict to **UNCERTAIN**, surfaced to
the user. Not auto-resolved by the loop.

User decision (surfaced UNCERTAIN + the 3 correct answers): **accept as-is, mark DONE** — build and
manual checks already passed; user chose not to re-quiz.

## Result
- Demo command (`npx tsc --noEmit && pnpm build`): **PASS**
- Manual success criteria (docs/PLAN.md 9.5.15 Verify): **PASS**
- Quiz-me gate: UNCERTAIN (0/3 answered) → **user accepted, closed as DONE**
- Milestone status: **DONE**
